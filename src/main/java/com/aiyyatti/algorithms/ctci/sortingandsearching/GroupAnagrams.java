package com.aiyyatti.algorithms.ctci.sortingandsearching;

/**
 * approach 1: sort it and used sorted as key and original as value O(N*log(M))
 * approach 2: a1b2c5 => as key - would be faster. O(N*M)
 */
public class GroupAnagrams {
}
